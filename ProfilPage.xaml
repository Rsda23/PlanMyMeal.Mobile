<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlanMyMeal_Domain.ProfilPage">
    <!--<Grid>
        
        <Frame Grid.Row="0" BorderColor="White" VerticalOptions="Start" HeightRequest="160" HasShadow="False" CornerRadius="20" Margin="0,45,0,20" Padding="0" IsClippedToBounds="True">
            <Image Source="timeline" Aspect="AspectFill"/>
        </Frame>

        <Label Grid.Column="0" FontAttributes="Bold" FontSize="18" HorizontalTextAlignment="Center">Profil</Label>
        <Grid Grid.Row="3" ColumnDefinitions="Auto,Auto,Auto,Auto,Auto" VerticalOptions="Center" HorizontalOptions="Center" Margin="0,20,0,0">
            <Button Grid.Column="0" ImageSource="location" WidthRequest="18" HeightRequest="18" CornerRadius="9" HorizontalOptions="Center" BackgroundColor="#9192b7"/>
            <Label Grid.Column="1" Text=" Venecy, Italy" FontSize="15" TextColor="#9192b7"/>
            <Button Grid.Column="2" ImageSource="age" Margin="10,0,0,0" WidthRequest="18" HeightRequest="18" CornerRadius="9" HorizontalOptions="Center" BackgroundColor="#9192b7"/>
            <Label Grid.Column="3" Text=" Age, 26" FontSize="15" TextColor="#9192b7"/>
            <Button Grid.Column="4" Text="Regular" TextColor="#e29389" FontAttributes="Bold" Margin="5,0,0,0" CornerRadius="10" FontSize="9" WidthRequest="{OnPlatform iOS='50', Android='59'}" HeightRequest="{OnPlatform iOS='21', Android='30'}" BackgroundColor="#fae6e4"/>
        </Grid>
    </Grid>-->
    <ScrollView>
        <VerticalStackLayout>
            <!--<Grid ColumnDefinitions="*, Auto" Margin="20, 0, 20, 0">
                <Label Text="Profil" FontAttributes="Bold" FontSize="18" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
                <Picker Title="Thème" SelectedIndexChanged="OnThemeChanged" HorizontalOptions="End">
                    <Picker.ItemsSource>
                        <x:Array Type="{x:Type x:String}">
                            <x:String>Clair</x:String>
                            <x:String>Sombre</x:String>
                        </x:Array>
                    </Picker.ItemsSource>
                </Picker>
            </Grid>-->
            <Grid ColumnDefinitions="*, Auto" Margin="20, 0, 20, 0" VerticalOptions="Center">
                <Label Text="Profil" FontAttributes="Bold" FontSize="18" HorizontalTextAlignment="Start" VerticalTextAlignment="Center" Grid.Column="0" />
                <Grid Grid.Column="1" Margin="0, 10, 0, 0">
                    <Label Text="Theme" FontAttributes="Bold" FontSize="14" HorizontalTextAlignment="Center" />
                    <Switch VerticalOptions="Center" HorizontalOptions="Center" Toggled="OnDarkModeToggled" Margin="0, 5, 0, 0" />
                </Grid>
            </Grid>
            <Frame WidthRequest="100"
                   HeightRequest="100"
                   CornerRadius="50"
                   Padding="0"
                   Margin="0, 20, 0, 0">
                <Image x:Name="ProfilImage" Source="user.png" HeightRequest="100" Aspect="AspectFill">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="ChangeImage"/>
                    </Image.GestureRecognizers>
                </Image>
            </Frame>
            <Label Grid.Column="0" FontAttributes="Bold" FontSize="20" HorizontalTextAlignment="Center" Margin="0, 10, 0, 10">Nom Prenom</Label>
            <Label Grid.Column="0" FontAttributes="Bold" FontSize="15" HorizontalTextAlignment="Start" Margin="20, 40, 0, 10">Coordonnées</Label>
            <Frame Margin="20, 0, 20, 20">
                <VerticalStackLayout>
                    <Label FontAttributes="Bold" FontSize="18" HorizontalTextAlignment="Start" Margin="0">E-mail</Label>
                    <Label FontSize="18" HorizontalTextAlignment="Start" Margin="0">exemple@gmail.com</Label>
                </VerticalStackLayout>
            </Frame>
            <Frame Margin="20, 0, 20, 20">
                <VerticalStackLayout>
                    <Label FontAttributes="Bold" FontSize="18" HorizontalTextAlignment="Start" Margin="0">Téléphone</Label>
                    <Label FontSize="18" HorizontalTextAlignment="Start" Margin="0">0601020304</Label>
                </VerticalStackLayout>
            </Frame>
            <Frame Margin="20, 0, 20, 20">
                <VerticalStackLayout>
                    <Label FontAttributes="Bold" FontSize="18" HorizontalTextAlignment="Start" Margin="0">Date de naissane</Label>
                    <Label FontSize="18" HorizontalTextAlignment="Start" Margin="0">1 janvier 2000</Label>
                </VerticalStackLayout>
            </Frame>
            <Button Clicked="Btn_Login" Text="Déconnexion"  FontAttributes="Bold" HeightRequest="45" FontSize="18" Margin="20, 0" />
        </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>